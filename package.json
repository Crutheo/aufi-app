{
  "name": "aufi",
  "private": true,
  "workspaces": [
    "shared",
    "api",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w api\" \"npm run dev -w frontend\"",
    "build": "npm run build -w shared && npm run build -w api && npm run build -w frontend",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w api && npm run typecheck -w frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "@esbuild/darwin-arm64": "^0.27.3"
  }
}
